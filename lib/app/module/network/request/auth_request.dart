

import 'package:rxdart/rxdart.dart';

import '../network_util.dart';


class AuthRequest {
  AuthRequest();

  /*Observable sign_in(params) => post(API_APP_LOGIN, params: params);

  Observable logout() => post(API_APP_LOGOUT);

  Observable loginComplete(params) =>
      post(API_APP_SIGNUP_COMPLETE, params: params);*/
}
